# Claudish - Development Notes

Planned-but-unimplemented work — the SEP-1686 channel migration, optional `notifications/progress` for terminal UI, the Anthropic plugin allowlist — lives in `ROADMAP.md`, each item with an explicit trigger condition.

**This file is the decision layer: what an agent must know *before* acting.** Detail is deferred, not deleted — each pointer below leads to the full text:

| Topic | Full reference |
|---|---|
| Routing syntax, provider shortcuts, `defaultProvider`, custom endpoints, vendor prefixes, local models | `docs/settings-reference.md` (§5–§7.5, §9, §12) |
| Three-layer adapter architecture (class-name Rosetta stone, per-layer detail) | `docs/three-layer-architecture.md` |
| `stream:false`, inline system messages, body capture, debug logging, translation-debugging workflow | `docs/reference/proxy-pipeline-notes.md` |
| Web search interception paths, SearXNG/MCP fallback chains, components | `docs/reference/web-search-interception.md` |
| Relay internals (header build, prober, compression, outage reconciliation) | `docs/reference/relay-internals.md` |
| Budget failover: cascade walk, notice channels, per-step backoff, Qwen/GLM thinking measurements | `docs/reference/budget-failover.md` · `.env.sidecar.example` |
| Negotiated capability vocabulary (#83 — wired: lift at `/v1/messages` entry + query at the condensation rail, gate `CLAUDISH_CAPABILITY_VOCAB` default off, ask cap `CLAUDISH_CAPABILITY_QUERY_MAX_ASKS` default 3) | `docs/reference/capability-vocabulary.md` |
| OpenAI-compatible ingress (`/v1/chat/completions`) request/response translation | `docs/reference/openai-ingress.md` |
| Traffic scripts, capture format, leak diagnostics | `docs/reference/traffic-analysis.md` |
| Channel wire format, client gating, tracing | `docs/reference/channel-mode.md` |
| Sidecar deployment runbook | `docs/deployment/relay-sidecar-deployment.md` |

## Fleet-Rollout Verification (hard rule, mandate 2026-09-12)

**A fleet-affecting change declared DONE requires a measured per-consumer verification artifact, produced in the same session** — for every client: where it actually lands, read from the live config/path, not from the change's intent. "The container moved" is not "the fleet migrated". The 2026-09-05 cutover had this and passed — then the ARR flip was silently reverted on 07/09 15:37 (byte-identical restore of the pre-flip `web.config`), and no cycle caught it until the 11/09 hub outage exposed it: 2 779 requests / 263.6M tokens served by the po-203 local cascade (106M on PAYG), invisible to the hub. Consequences, now standing:

- **Surveillance covers the path, not just the traffic**: a cycle checking hub health also checks the entry points that feed it (ARR backend, per-machine base URLs) — a silent revert must be catchable within one cycle, not one outage.
- **An outage detected is an outage escalated — in the SAME cycle** (DM URGENT + dashboard). "Chronic" thresholds (#80-style counts) never apply to a total outage.
- **An issue treated halfway is an issue in failure**: no next grain until the current issue's DoD is met or the rescope is explicitly recorded.
- Rollback of a fleet change goes through the same channel as the change: an unlogged revert of shared infra (IIS web.config, .env, NAT) is an incident, not a fix.

## Build, Release, Versioning

- `bun run build` (CLI + macOS bridge bundles) · `bun run dev`. Use **bun**, never npm or yarn.
- **Releases are handled by CI/CD — never run `npm publish` by hand.** Bump version → conventional commit → `git tag -a vX.Y.Z -m "…"` → `git push origin main --tags`.
- **Every PR that resolves an issue carries `Closes #NN` in its body.** GitHub only auto-closes on merge when the keyword is present: a descriptive "Issue #79 asked for…" leaves the issue open with its fix already merged and live, and the registry drifts silently. Measured 2026-09-12 — #65, #79 and #80 were all in exactly that state (three merged fixes, three orphaned issues).
- **A version bump touches three files, all mandatory**: root `package.json`; `packages/cli/package.json` (**this is what CI publishes — stale here and `npm publish` fails**); `packages/cli/src/version.ts` (fallback `VERSION`, so compiled binaries with no package.json still report correctly).

## Model Routing (v4.0+)

Syntax `provider@model[:concurrency]` — `google@gemini-2.0-flash`, `or@deepseek/deepseek-r1`, `ollama@llama3.2:3`. Bare names auto-detect (`gpt-4o` → OpenAI, `gemini-*` → Google). Shortcuts (`g@ oai@ or@ openrouter@ mm@ mmax@ mmc@ kimi@ moon@ glm@ zhipu@ gc@ llama@ ll@ litellm@ ollama@ lmstudio@` + aliases) plus any custom-endpoint name: full table in `docs/settings-reference.md` §5.2 — except **`cx@` / `codex@` → OpenAI Codex (Responses API), which that table does not list**; this file is its only record.

Gotchas:

- **`defaultProvider` is a last-resort fallback, not a front-of-line override.** It is appended to the *end* of each bare-name chain (deduped — `or` ≡ `openrouter`) and only catches models whose chain has zero credentialed providers. Explicit `provider@model` is never affected. Precedence: `--default-provider` > `CLAUDISH_DEFAULT_PROVIDER` > config `defaultProvider` > `OPENROUTER_API_KEY` present > hardcoded `openrouter`.
- **LiteLLM auto-promotion was removed** (commit 5 of the model-catalog/routing redesign; see `default-provider.ts`). `LITELLM_BASE_URL` + `LITELLM_API_KEY` no longer makes LiteLLM the default — set `defaultProvider: "litellm"` explicitly. ⚠ `docs/settings-reference.md` §6.1/§6.3 still documents the legacy promotion; that text is **stale — trust the code**.
- **Vendor prefix resolution is exact-match only** — find the right prefix, never guess the model. Dynamic provider catalogs are PRIMARY; `OPENROUTER_VENDOR_MAP` is a cold-start fallback only. Resolution happens in `proxy-server.ts` **before** handler construction, through the sync `resolveModelNameSync()` (in-memory caches + `readFileSync` — do not make it async).
- **Custom endpoints**: `apiKey` expands `${VAR_NAME}` from the environment (never hardcode secrets); entries failing Zod validation **emit a stderr warning and are skipped — they must never crash the proxy**. Endpoints inject themselves through `registerRuntimeProvider()` / `registerRuntimeProfile()` (`providers/custom-endpoints-loader.ts`); the full `customEndpoints` schema (`modelPrefix`, `CLAUDISH_CONFIG_DIR`, per-endpoint key vars) is in `docs/settings-reference.md` §7.
- **`omitReasoningContent`** (custom endpoints, default `false`) drops `reasoning_content` from outbound assistant messages, for backends that validate their body strictly. It is needed because the OpenAI converter emits that field whenever history holds a thinking block — **independent of any opt-in**, since the consumers disagree: DeepSeek *requires* it echoed back, Kimi K2.5 needs it from turn 2, GLM ignores it. Mistral answers `HTTP 422 extra_forbidden` and fails **every** turn of a thinking-mode session. ComposedHandler's thinking-block strip cannot reach it: on the OpenAI wire that strip is a **structural no-op** (it runs after `convertMessages`, by which point content is flattened and reasoning hoisted to a sibling scalar) — hence a separate `stripReasoningContent()` in `openai-messages.ts`. Incident of 2026-08-20 and the measurement that Mistral's `zai-glm-5-2` emits no traces at all: `docs/reference/proxy-pipeline-notes.md`.
- **Local model APIs report `prompt_tokens` as the FULL conversation context each request**, not a delta. `writeTokenFile` therefore assigns (`=`) input tokens rather than accumulating (`+=`).
- **The three input counters ship TOGETHER on every terminal `message_delta`, and a zero `input_tokens` is a trap (#179).** `splitPromptTokens()` (`stream-parsers/usage-cache-split.ts`) derives `input_tokens` / `cache_read_input_tokens` / `cache_creation_input_tokens` from `prompt_tokens` in ONE place, clamped so the three always sum back to it — because **Claude Code reconstructs the conversation size by summing them raw**, with no coalescing (verified in the shipped bundle, identical in 2.1.17 and 2.1.273: `input_tokens + cache_creation_input_tokens + cache_read_input_tokens`, feeding the occupancy percentage). Two consequences that look like style and are not: (a) emitting a reduced `input_tokens` **without both siblings** understates the context by exactly the cached share — the shape #99 was opened for, which silently disarms auto-compaction; (b) `message_start` must seed both cache counters at an explicit **0**, because the client's per-key merge takes a delta only when it is `> 0` (`K.x !== null && K.x > 0 ? K.x : A.x`) and an absent seed key therefore returns `undefined`, turning that raw sum into `NaN` — `output_tokens` uses `??` and coalesces, the three input counters do not. Hence the **`fullyCached` unsplit rule**: on a turn whose input is entirely cache, `input_tokens: 0` would be *discarded* and the previous turn's seed kept beside a full-size `cache_read`, roughly doubling the reported context — so that turn ships as ordinary input instead. Our `toAnthropicUsage` has **4 call sites** (all `message_delta` → `message_stop`, incl. the #65 policy-refusal path), which is wider and more consistent than upstream's single inlined site. ⚠ Read counters by **observed** spelling, never invented: `prompt_tokens_details.cached_tokens` *and* DeepSeek's top-level `prompt_cache_hit_tokens` (`??`, not `||` — a reported 0 has spoken), plus `cache_write_tokens` for creation (no producer on our lanes: 0 occurrences measured). Cost rides in a separate `detail` argument and is **inert today** — `cacheReadCostPer1M` has zero producers, so every discount computes to exactly 0. ⚠ **The Codex lane (`openai-responses-sse.ts`) still emits the pre-#99 shape on 6 sites and is NOT covered — #186.**

Adding an aggregator resolver: implement `ModelCatalogResolver` in `providers/catalog-resolvers/`, register in `model-catalog-resolver.ts` — proxy-server and provider-resolver need no changes. The hosted slim catalog's `aggregators[]` field — entries of `{ provider, externalId, confidence }` — is a typed multi-provider routing index, consumed read-only; extraction, recommendations and portal live in [models-index](https://github.com/MadAppGang/models-index). Design notes: `ai-docs/sessions/dev-arch-20260305-104836-a48a463d/architecture.md`.

## Adapter Pipeline (v5.14.0+)

`ComposedHandler = FormatConverter (explicit adapter) + ModelTranslator (auto-selected by model id) + ProviderTransport`

- **L1 FormatConverter** (`adapters/api-format.ts`) — wire format: OpenAI, AnthropicPassthrough, Gemini, Codex, OllamaCloud, LiteLLM. Message/tool conversion in `handlers/shared/format/openai-messages.ts`, `openai-tools.ts`.
- **L2 ModelTranslator** (`adapters/model-dialect.ts`) — model dialect (context windows, thinking→reasoning_effort, vision rules): GLM, Grok, MiniMax, DeepSeek, Qwen, Codex.
- **L3 ProviderTransport** (`providers/transport/types.ts`) — auth, endpoints, headers, rate limits; aggregators (LiteLLM, OpenRouter) may override the stream format to `openai-sse`.

**Stream parser selection — 3-tier priority, in this order:**

```typescript
transport.overrideStreamFormat() ?? modelAdapter.getStreamFormat() ?? providerAdapter.getStreamFormat()
```

Parsers in `handlers/shared/stream-parsers/`: `openai-sse` (most providers), `anthropic-sse` (MiniMax/Kimi direct), `gemini-sse`, `ollama-jsonl`, `openai-responses-sse` (Codex).

- **Add a provider**: one entry in `PROVIDER_PROFILES` (`providers/provider-profiles.ts`).
- **Add a model**: a ModelTranslator adapter, registered in `adapters/dialect-manager.ts`.
- **Verify wiring**: `claudish --probe <model>` prints the full composition.

## Never-Hang Constraints (non-negotiable)

The proxy sits inside an agentic loop: whatever stalls a turn stalls the agent.

- **A restart is a stream killer, so the drain waits for a true zero first, then picks its moment.** `docker restart` cuts every in-flight SSE mid-body and each client reports `Connection lost mid-response` — that agent turn is lost. An initial 30-min measurement (2026-08-23, 906 samples) saw `activeStreams` never reach 0 (min 1; p50 5, p90 7, mean 5.1) and produced the first design — relative target, no zero wait. A ~2-day population probe (2026-08-25, 92 364 samples) corrected that window: **min 0, 4.62% of samples at zero, P(zero within 300s) = 57.5% (73.4% within 600s)** — the floor IS zero, lulls are just brief (mean 5.9s). `scripts/claudish-drain.ps1` therefore holds a true-zero wait for the first 300s (`$target` stays -1, only `activeStreams==0` fires — on the FIRST zero sample, no confirmation: confirming costs a whole lull), then falls back to the adaptive stage for the ~43% unfavorable draws: observed minimum as target, relaxed by one every 30s so the wait ends on a chosen moment rather than on the cap. Replay of the 2026-08-23 samples through the adaptive stage alone: **mean 5.19 → 3.65, worst case 10 → 6**; relaxing once per poll instead of every 30s gives the gain straight back (4.67). Budget: `$MaxWaitSec=600` (300s zero-wait + ~300s adaptive) — the 04:00 daily restart can afford 10 min. Even so, **restarting less often remains the mitigation that matters more than draining better** — `ClaudishDailyRestart` (04:00) has run the drained wrapper since 2026-08-23 (first drained restart: 08-24 04:00, logged in `~/.claudish/drain.log`), and whether it needs to run at all is the open question. A `$null` count means *no signal* (proxy down, or image predates #37) and must degrade to an undrained restart, never to an unbounded wait.
- **The proxy itself never breaks a stream**, and that invariant is what makes the above diagnosable: there is no `controller.error()` anywhere in the proxy's **production** sources under `packages/cli/src` (test harnesses legitimately call it — see the filter below), and every terminating path emits `message_stop` — including `finalizeWithError` on an upstream socket death and the relay passthrough on a hub death. Corpus re-check 2026-09-20, ai-01, **1017 captured `resp-*.sse` over 10/08→19/09** (3.7× the original 277 sample): **1016 of 1016 non-empty responses carry a terminal `message_stop`** — and the 1017th is **0 bytes**, so it carries no events at all and is classifiable in neither direction. That empty-capture class is the trap, not the invariant: a `grep -L 'event: message_stop'` reports it as "1 response without a terminal event", which reads exactly like a never-hang violation. So a client-visible mid-response drop is never the stream logic; the socket died underneath it.

  ⚠ **Checking the `controller.error()` half needs a filter that drops comments *and* tests — and this paragraph's own earlier filter was insufficient.** The instrument, re-measured 2026-09-20 on `main`:

  ```bash
  grep -rn 'controller\.error(' packages/cli/src --include='*.ts' \
    | grep -v '\.test\.ts' | grep -v ':[0-9]*: *\(//\|\*\)'   # → 0
  grep -rn 'controller\.close(' packages/cli/src --include='*.ts' \
    | grep -v '\.test\.ts' | grep -v ':[0-9]*: *\(//\|\*\)'   # → 24  (positive control)
  ```

  The two weaker filters both mislead, and the numbers this paragraph used to quote have drifted while the invariant has not: a **bare** grep now returns **8**, not the "2 hits, both prose" written here earlier (3 comments — `stream-registry.ts:13`/`:91` plus `anthropic-sse-s4a.test.ts:65` — and 5 real calls); **comment-excluding alone returns 5**, every one of them in a `*.test.ts` harness that deliberately errors a `ReadableStream` to simulate a mid-stream socket death, which is *how the never-hang behavior is tested*. So a reviewer following the filter this doc prescribed got **5** and had grounds to report the invariant broken — the exact false conclusion the paragraph exists to prevent (raised against PR #166 on 2026-09-20, then refuted by re-running the same filter on `main`: identical 5, so not the PR's). **Before concluding from a count, run the same filter on the merge base** — a hit class that predates the change is not the change's. The 24 `close()` calls are the positive control: they prove the filter still matches the pattern rather than silently matching nothing. A doc that invites a check owes the check its instrument — and owes it a scope (`production sources, tests excluded`), since an unscoped number goes stale the first time a legitimate new class of hit appears.

- **`stream: false` must return a single JSON `message`, never SSE.** `/compact` is the real-world caller; SSE back to it surfaces as `"API returned an empty or malformed response (HTTP 200)"`, the session can no longer condense, and it eventually overflows and stalls. `ComposedHandler.handle()` buffers the already-translated SSE via `collectAnthropicSseToMessage()`. The trigger is exactly `payload?.stream === true` (mirrors `request-logger.ts`); anything else buffers to JSON. The collector **never throws** — a broken or empty stream degrades to a well-formed message with an empty text block.
- **A failed web search/fetch must never stop the agent** — every backend path degrades to well-formed text. No throws, no hung streams.
- **Never gzip the SSE response** — gzip buffering risks a hang. Request body only, WAN only.
- **A stream admitted with 200 that never produces a first useful event is confined, not held** — during the 14/09 deepseek-flash scheduler incident, mute-but-keep-aliving streams held hub slots ~900s each (46 streams / 19 pending at peak). `withFirstUsefulEventWatchdog` (wired in `ComposedHandler.handleStream`, all SSE formats, not ollama-jsonl) cancels the upstream after `CLAUDISH_FIRST_EVENT_TIMEOUT_MS` without a first `data:` line (default 300s, `0`=off, re-read per request); the parser's existing synthetic finalization then ends the client turn cleanly. The window covers the FIRST useful event only — a `: keep-alive` comment or `event:` line is not one — so legitimate long thinking (whose deltas are data events) can never trip it.
- **A retried stream needs its OWN watchdog — the original wrap has already disarmed.** The wrap above clears its timer on the first `data:` line and *never rearms*; an in-stream error chunk (`data: {"error":…}`) is itself such a line, so by the time it triggers a transparent retry the protection is gone, and a bare `doFetch()` replacement would be unbounded — a mute-but-200 retry upstream then hangs the client exactly as #108 confines. `boundRetryUpstream()` (`first-event-watchdog.ts`) wraps the closure once in `handleStream`, so all three retrying lanes inherit it; non-ok/bodyless responses pass through untouched, and `ollama-jsonl` is excluded from both wraps (that wire has no `data:` lines, so wrapping it would fire on every healthy stream). Measured in review of #137: with a 1 s window in force and the wrapper removed, the client stream was still open after 6 s; with it, the turn terminates at the window. Corollary for any future recovery path: **a guard that disarms on a signal cannot protect what that same signal triggers.**
- **An upstream death before any client-visible event is recovered, not finalized (#170).** Never-hang was already satisfied by `finalizeWithError` — the turn ended cleanly — but it ended on a *notice instead of an answer*, and nothing had been forwarded that would prevent re-issuing the request. `anthropic-sse.ts` now re-forwards on both death shapes (`reader.read()` throwing, **and** a graceful `done` having emitted nothing — `docker stop` on the upstream produces the second), gated by the same predicate #65 uses for policy refusals: `!sawMessageStart && highestSeenIndex === -1 && !lastBlockOpen`. Past any of those three a replacement stream would duplicate what the client holds. Bound + kill switch: `CLAUDISH_PREVISIBLE_REFORWARD_MAX` (default 2 = the `[400, 1200]ms` ladder, `0` = off, re-read per stream), on its own counter so it composes with the policy retry without either becoming unbounded. The relay supplies the closure (it passed none before — the opts comment read `absent = inert (relay, tests)`), header-bounded and **wrapped in `boundRetryUpstream()`**: the relay bypasses `ComposedHandler`, so there is no outer watchdog to inherit and a mute-but-200 replacement would hang the client — mutation-proven, wrap removed ⇒ the stream was still open at 20 s. A re-forward is a recovery attempt, never a liveness measurement: **no `markFail`**, same separation as the #80 part 2 connect retry. Accepted knowingly: the upstream may have generated tokens that never reached us, so a re-forward can bill twice — the bound is what keeps that cheap. Driver, 2026-09-20: 13 hub container restarts in one morning (a remediation for a forwarder wedge that restarts cannot fix) cut every in-flight SSE on every relaying machine at 9-13 `activeStreams` each; **the fleet-wide turn loss came from the remediation, not the wedge.**
- **A policy refusal is retried, then surfaced labeled — never bare, never silent (#65).** `invalid_prompt`-class refusals (exact code on OpenAI-shaped wires; usage-policy message pattern on anthropic-compat wires that carry no code — Z.AI/MiniMax/Kimi) get a bounded transparent retry of the *identical* body (`[1000, 3000]ms`, max 2), fired **only while nothing client-visible exists** — stricter on `anthropic-sse`, which also requires no forwarded `message_start` since that event comes from upstream there and a retry past it would duplicate it. A persistent refusal becomes `[Upstream policy refusal — invalid_prompt: …]` + `end_turn` + `message_stop`. The classifier deliberately excludes transport errors, quota walls and rate limits, so it can never divert a subscription wall away from `isQuotaExhaustion`. `[PolicyRefusal] lane=… model=… provider=… attempt=… action=retry|surface` (forceConsole) is the countable marker for #41/#60. Before this, `openai-sse` **dropped in-stream `{"error":…}` chunks entirely** — a refusal on most providers' lane surfaced as an unexplained "empty response".
- **The failover notice never throws** — a thrown error would turn a working condensation into a failed one.
- **Strip inline `role: "system"` messages** (Claude Code v2.1.153+ injects them, e.g. system-reminders) and merge into the top-level `system` field — for `anthropic-sse` in `composed-handler.ts` and for the OpenAI path in `openai-messages.ts`. Z.AI, MiniMax and Kimi reject anything but `user`/`assistant`.

## Web Search Interception (v7.1+)

Provider web-search tool calls (`web_search`, `brave_web_search`, `tavily_search`) and GLM `<searchWeb>` tags are intercepted, never forwarded (they would fail on non-Anthropic providers).

**The decision that matters**: if the client declared a `WebSearch` tool, **remap** the call to a synthetic `WebSearch` tool_use block with `stop_reason: "tool_use"`, so Claude Code runs its own search and the loop continues. Only when WebSearch is *not* declared (sub-agents without web tools) do we suppress and inject results as a text block with `end_turn`. Suppress-and-inject in the declared case ends the assistant turn on raw search results and **stalls the agent** — the CoursIA incident, 2026-06-10. Regression: `format-translation.test.ts` ("web search remap").

**On the Z.AI wire, `server_tool_use` comes with two siblings** (live capture 2026-08-11, `regression-zai-server-tool-use-live.sse`): the tool **input arrives whole in `content_block_start`** (zero `input_json_delta` — delta-based accumulation left `rawInput` empty on every real stream), and the provider also emits a **`tool_result` block assistant-side** after executing the tool server-side. `tool_result` is user-turn-only on the Anthropic wire, so it must be suppressed exactly like `server_tool_use` (the same results arrive as the preceding text block) — and because that removes a second block, the parser must remap the following deltas/stops down or indices desync ("Content block not found"). Regression: `format-translation.test.ts` ("Chantier D", structural asserts).


**Any frame the parser drops takes its `event:` line and blank separator with it** (S4-a): an `event:` header that reaches the client without its `data:` body kills the turn with `Could not parse message into JSON` — so `event:` lines are held until the data line is adjudicated, on **both** branches (thinking filter and passthrough). The index-mapping layer (jumped starts remapped, the pair remembered so deltas/stops follow) also runs on **both** branches — MiniMax (`shouldFilterThinking()` = true) takes the filtered one — and an **orphan delta/stop** (no block the client opened at that index, e.g. MiniMax-M3's implicit signature block: `signature_delta` + stop at 0 with no `content_block_start`) is **dropped whole**, never re-attached. Synthetic tails (`finalizeWithError`, empty-stream finalization) are built with `JSON.stringify` — never string interpolation of the model name — preserve an upstream-reported `stop_reason`, and never emit a second terminal pair after a real `message_stop`. Production fixtures + pins: `anthropic-sse-s4a.test.ts` (`minimax-m3-anthropic-implicit-signature-r10324/r10416.sse`).

Backends, first usable result wins: MCP `searxng_web_search` (`SEARXNG_MCP_URL`, ≥ 5s) → direct HTTP `{SEARXNG_URL}/search?format=json` (**2 attempts inside an 8s total budget, 5s/attempt** — dimensioned from measured p50 0.92s / p95 2.97s, roo-extensions #3388; `SEARXNG_ATTEMPT_TIMEOUT_MS` overrides) → error text; fetch adds an `r.jina.ai` retry for bot-hostile hosts and a 500KB cap. Every call is deadline-bounded, and direct-HTTP attempt durations feed a rolling 50-sample p50/p95 log (`[WebSearch] latency window: …`) for drift detection. **Basic-auth credentials belong in the `SEARXNG_URL` userinfo** (`https://user:pass@host`, standard curl form): `searxngConfig()` parses them into an `Authorization: Basic` header and strips them from the logged base URL. `search.myia.io` sits behind IIS Basic Auth since 2026-08-19, so credential-less WAN clients get 401; LAN deployments reach the backend directly and send no header. `MCP_AUTH` / `SEARXNG_MCP_TOKEN` are provisioned via RooSync — never hardcode. When `SEARXNG_MCP_URL` is unset the MCP layer is skipped entirely (zero behavior change). Detail: `docs/reference/web-search-interception.md`.

## Relay / Sidecar Mode (v7.2+)

One binary, three modes, selected by `CLAUDISH_RELAY_UPSTREAM`. Each machine runs its own container so the hub stops being a single point of failure.

| `CLAUDISH_RELAY_UPSTREAM` | Mode | Behavior |
| --- | --- | --- |
| **unset** | **HUB** (po-2023) | Always local. Zero change vs. before. |
| set + hub **alive** | **NOMINAL relay** | Forwards the raw request to the hub; response repiped through the never-hang passthrough. **No local capture** (hub captures centrally). |
| set + hub **dead** (hysteresis) | **AUTONOMOUS** | Falls through to the normal local pipeline + local capture. |

Gotchas:

- **The cluster key is injected as `x-proxy-key`, NOT `x-api-key`**, and the client `authorization` is kept. `x-api-key` arms `NativeHandler`'s proxyKey→Anthropic swap, which strips auth → **401**. `X-Claudish-Machine` must survive the hop or central attribution is lost.
- **Hysteresis is asymmetric on purpose**: 2 consecutive `/health` failures → AUTONOMOUS (fast failover); recovery needs 3 OK heartbeats **+ 60s cooldown + a deep tool-call probe** (`glm-5.3` — the current sonnet nominal — must reach `message_stop`). Anti-flap.
- **`CLAUDISH_NO_ANTHROPIC=1` on every machine except ai-01** — reroutes bare native targets to the budget `modelMap.sonnet` instead of `api.anthropic.com`. Defense in depth, not the primary policy.
- **`CLAUDISH_HOST_PORT` is load-bearing, not cosmetic**: port 3000 is already taken on some hosts (ai-01), so a hardcoded binding makes `docker compose up` fail outright. The container side always stays 3000.
- **Two capture variables, one letter apart in meaning and deliberately not in spelling**: `CLAUDISH_CAPTURE_HOST_DIR` (host bind path) vs `CLAUDISH_CAPTURE_DIR` (in-container path, **empty = write nothing**). Keep capture **on** for sidecars: a forwarded request writes nothing, so any file that appears IS a forward-path outage capture — the trail `reconcile-outage-captures.ps1` needs. But **capture files are not a count of mode switches**: capture follows *local serving*, and a request served locally because its own forward failed (`header-timeout after 30000ms`, relay still NOMINAL) captures just as an AUTONOMOUS one does. Measured on po-203, 2026-09-13: **68 loose files over 2h20 against a single 71s AUTONOMOUS episode in the surrounding 28h** — reading files as mode switches over-counts by ~30×. The `-direct` / `-openai` field in the filename is the handler label, not the mode. Disabling capture is a disk-starvation escape hatch that silently destroys that trail.
- **An armed cascade is always hand-added, so it can live only in the container record (#141, fixed).** `install-sidecar.ps1` generates the per-machine `.env` with exactly `CLAUDISH_PROXY_KEY` / `CLAUDISH_RELAY_UPSTREAM` / `CLAUDISH_CONFIG_DIR` / `CLAUDISH_CAPTURE_HOST_DIR` / `CLAUDISH_HOST_PORT` / `CLAUDISH_CONTAINER_NAME` (+ optional `COMPRESS`/`NO_ANTHROPIC`/`CAPTURE_DIR=`) and **never a single `CLAUDISH_FAILOVER_*`** — arming is a provisioning act, not an install one. Until #141 the rewrite also **dropped** any that were there, so an armed state survived on `unless-stopped` luck alone; since #141 the installer carries runtime-policy lines over verbatim, **exits non-zero before writing** an `.env` that would gut a live armed cascade (`claudish-drain.ps1 -Recreate` refuses the same condition), and `-RebuildEnvFromContainer` recovers a state that exists only in the container. The 07/09 hub wipe was therefore not an operator slip but the tool's default outcome — the hub only survives because po-2025 maintains `D:\claudish-shadow\.env` by hand, and that file carries **17 armed** of the 23. **Before any sidecar recreate: diff the target env file against `docker inspect`'s `.Config.Env` on NON-EMPTY assignments, and reconstruct the file first** (its output carries `CLAUDISH_PROXY_KEY` — local file only, never committed, never printed). ⚠ **Diff by value, not by name**: every container shows all 23 names whatever its state, so a name diff answers a different question than the one asked — it was wrong about ai-01 (reported armed, actually **0 non-empty**, 2026-09-19), and it is what makes a partially-filled file deceptive (po-203: 16 lines in the file look recreatable, and whether the 7 absent names matter depends entirely on whether they hold values). `install-sidecar.ps1 -RebuildEnvFromContainer` does this correctly and is the tool to use rather than a hand-rolled grep.
- **The relay branch must stay before `interceptWebTools` / `getHandlerForRequest` / `logRequest`** in the `/v1/messages` route — that ordering is what makes capture mode-aware for free.
- **A forward connect failure gets exactly one retry (+250 ms) before falling through (#80 part 2)** — the container → `host.docker.internal` tunnel resets chronically (~1 per 8 min measured over 12 h, 87 failures, while the hub answered `/health` in 4 ms: the defect is the Docker Desktop transport, not the hub). A connect failure fails in milliseconds, so the retry absorbs the blip at no perceptible cost, and an **absorbed retry does not `markFail`** — the chronic blips no longer feed the AUTONOMOUS hysteresis nor divert requests to the local cascades (invisible to the central capture, and potentially PAYG where the hub's subscriptions would have served). A header deadline is never retried (that would double the 30 s stall), nor is an HTTP 5xx (the hub answered; hysteresis owns it). `[Relay] forward connect failed (…) — retrying 1/1` is the countable marker.
- **`hub HTTP 500` is the *relay's* reading, and until #212 the hub could not corroborate it — read the hub's own stall signature before hunting a provider.** `relay.ts` maps **any** `res.status >= 500` to `markFail(forward-http-${status})` and so arms the AUTONOMOUS hysteresis on the status alone, whatever produced it. Measured 2026-09-22 `03:18:02→03:19:48Z` (hub po-2025, 106 s): **18 requests across 6 machines (po-2023…po-2027, ai-01) and 4 models** got no response at all, the hub logged **nothing** for any of them — no `[ttft]`, so no upstream was ever reached — while its per-minute throughput collapsed `32 → 4 → 1 → 24`. Two exclusions to reuse rather than re-derive: the compaction (its `compaction.log` is **UTC**: `01:50:15Z→02:43:22Z`, ended **35 min before**) and the drained restart (`02:03:01Z`, **1h15 before**, 0 streams cut). The decisive evidence was the **host's** log, not the proxy's: `Tcpip` **4231 ephemeral-port exhaustion** (1×/day on 4 of the last 7 days, dynamic range at the Windows default 16384, `TcpTimedWaitDelay` unset) and `DNS-Client` **1014**. The general instrument rule this leaves behind: the hub's `/v1/messages` and `/count_tokens` 500 branches logged through `log()` — **file-only, a no-op with debug off, which is how a hub runs** — so *an empty hub log was never evidence of a healthy hub*; since #212 both branches emit `[Proxy] ERROR 500 on POST <path>` via `logStderr`.

Tests: `relay.test.ts` (**45 as of 2026-09-20** — hysteresis, header build incl. ai-01 Opus passthrough, gzip, never-hang delegation, pre-visible re-forward; the "16" written here previously had drifted to 40 before #170 added 5, so treat a bare count as dated or don't write one). Budget-free E2E: `bun run packages/cli/src/fork/server/relay-e2e.ts` (~2-3 min). Internals: `docs/reference/relay-internals.md`. Fleet install: `scripts/install-sidecar.ps1` + `docs/deployment/relay-sidecar-deployment.md`.

## Budget Failover (v7.2+)

Substitutes the *model itself* for a whole **role** (`opus`/`sonnet`/`haiku`/`fable`) when that role's metered plan is exhausted — a subscription concern, distinct from `FallbackHandler`, which swaps *providers* for the same model (a transport concern). They compose. Every substitution is **announced**, because silent degradation leaves the agent assuming capabilities it lost (or missing ones it gained).

**Three notice moments, two channels**, centralized in `applyFailoverNotices` so `ComposedHandler` *and* `NativeHandler` are covered: at **condensation** (appended to the `/compact` message — the one moment context is rebuilt anyway — naming the currently-resolved step and its depth); at the **moment of failover** (prepended as stream content block 0, every real block's `index` shifted `+1`, re-fired when the resolved step changes mid-session); and on **recovery** (symmetric, so the model recalibrates after the nominal returns). None of the three may throw — a missing notice beats a broken stream, and a thrown error would turn a working condensation into a failed one.

> ⚠ **Notices state facts, never instructions — doctrine 2026-08-23.** The recovery notice originally told the agent to "resume your normal capability and **risk appetite**" and to "clean up any over-conservative decisions made under the substitute". Arriving unexplained in content block 0, that was indistinguishable from a prompt injection: it was reported as one on 2026-08-23 by a fleet agent whose security reflex fired correctly, before being traced back to our own code. Rewritten factual in #126 ("Capability note: …", plus a "Remaining fallbacks: …" depth enumeration after the step being served). Standing rule for any new notice: which role, which model, which direction — state capability or scope, never risk posture, and never ask an agent to undo decisions it already made.

**A role's failover is an ordered list of steps, not a single target.** `CLAUDISH_FAILOVER_<ROLE>` is `>`-separated: step 0 serves when the nominal walls, step 1 when step 0 *also* walls, and so on; the last step is typically PAYG, which has no weekly wall. Opus→GLM is not a special route — it is Opus→Qwen with Qwen also dead, falling through to Qwen's own successor. GLM is a **rolling ~5h window** (it dies *and* restarts every 5h, unlike Qwen's consumable weekly pool), so PAYG naturally fills GLM's holes with no extra routing.

Env, read at proxy startup (canonical annotated template: `.env.sidecar.example`): `CLAUDISH_FAILOVER_<ROLE>` (the `>`-separated cascade), `..._LABEL`, `..._DIRECTION` (`degraded` default · `improved` · `lateral`), `..._NOTE`, `..._RESET` (operator-declared wall-lift per step, ISO 8601), `CLAUDISH_FAILOVER_ROLE_MODELS` (aliases so clients naming the nominal model directly still get cascade protection), `CLAUDISH_FAILOVER_ACTIVE`, `CLAUDISH_FAILOVER_AUTO`, and the #91 grace-before-arming knobs `CLAUDISH_FAILOVER_ARM_AFTER` (consecutive qualifying nominal refusals before arming; 1 restores arm-on-first-refusal) · `CLAUDISH_FAILOVER_ARM_GRACE_MS` (wait-and-retry the nominal once in-request) · `CLAUDISH_FAILOVER_ARM_RETRY_AFTER_CEILING_MS` (a 429 naming a retry-after below this is a burst and never arms — a wall speaks in hours) · `CLAUDISH_FAILOVER_SESSION_DWELL_MS` (minimum time a session keeps its resolved step; see the dwell bullet below). All `>`-separated fields are position-preserving against the step list.

- **With nothing set every code path is inert.** Configuring a target does **not** activate it — arming is separate and deliberate.
- **Unset `_DIRECTION` means degraded** — never flatter the substitute.
- **`isQuotaExhaustion` is deliberately narrower than `FallbackHandler.isRetryableError`**: 402 arms on status alone; 429 arms *only* when the body names a quota/credit/balance/weekly/plan wall — a per-minute burst must not burn the weekly switch. **401 and 404 never arm**: those are wiring mistakes, and swapping the model would hide a bad key or bad model id behind a plausible-looking answer.
- **`resolveFailoverTarget(role)` is the single resolution path** — used by both the swap in `getHandlerForRequest` and the `handleWithCascade` loop. The loop never passes an override target in; it mutates failover state and the next iteration re-reads. No double resolution.
- **`handleWithCascade` is bounded, never `while(true)`**: nominal → step 0 → step 1 → …, capped at `steps.length + 1` attempts. The **c-reuse invariant** is load-bearing — a handler must not mutate the Hono `Context` before returning a non-ok `Response`, or re-calling `handler.handle()` across iterations breaks.
- **Per-step backoff `[10m, 30m, 1h, 4h, 24h]`, and a known reset time overrides it entirely.** Re-probing a 24h-capped cycle against an 11-day wall is pure waste, so `..._RESET` (or a date parsed from the body — Qwen names its reset, MiniMax counts down) holds the step skipped until that instant, then makes it probeable the moment it passes. **`stepFailures` survives the role-arm TTL cycle**: without that, the 10-min nominal re-probe would re-probe a weekly-walled step every 10 minutes, defeating the backoff. It IS cleared for the whole role when the *nominal* recovers — a healthy nominal means a fresh episode.
- **The role-arm TTL itself grows while the wall holds (10 m → 20 m → 40 m, capped)** — a flat 10-minute disarm/re-arm oscillated for the whole duration of a wall: ~6 provider switches per hour, each flipping the serving model twice with a cold prompt-cache at both ends (measured on the hub: 107 arms ≈ 214 model transitions per 24 h, #91). Each TTL expiry that re-arms escalates the next arm's TTL; any nominal success resets it (fresh episode); a re-arm more than 40 min after the last disarm starts back at base. `[Failover] ARMED … (ttl Nmin, escalated)` and `DISARMED … next arm TTL grows to Nmin` keep the hub log countable (#91 point 3).
- **A session keeps its resolved step for a minimum dwell** (`CLAUDISH_FAILOVER_SESSION_DWELL_MS`, default 10 min, `0` = off) — the role-level dampers bound how often switches *happen*; the dwell bounds how often *one conversation* rides them. `resolveFailoverTargetForSession` (the routing seam for `getHandlerForRequest` and the cascade loop) pins a session to its step: the pin **holds** while the step is servable, even as the role-level state churns (disarm→nominal probe→re-arm — that oscillation moves traffic only *between* conversations), and **yields** on genuine advancement (the pinned step itself TTL-failed). The dwell is *sliding*: an active session renews it every request; an idle session's pin lapses after the window. Nominal is never pinned — pinning it would feed a session back into the wall its own refusal just armed (#91 point 4).
- **A failover target must resolve on the machine that calls the models.** Pointing a sidecar at a custom endpoint defined only in the hub's `config.json` configures a fallback that fails exactly when it is needed. The hub is the normal home — but **"only in AUTONOMOUS mode" is not the same as "rarely"**. ⚠ **The ai-01 sidecar is NOT armed — and a count that says otherwise is counting names.** `docker inspect` shows **23 `CLAUDISH_FAILOVER_*` present and 0 non-empty** (re-measured 2026-09-19). Compose injects all 23 with a `${VAR:-}` default, so **they exist whether or not anything is armed**: `grep -c '^CLAUDISH_FAILOVER_'` on a container's env returns 23 on every machine and can never answer the question. A 2026-09-18 edit here claimed "it is armed now — 23 in the live container" on exactly that miscount; it is **withdrawn**. What refuted it is `install-sidecar.ps1 -RebuildEnvFromContainer` (#141), whose `Get-ArmedCascadeCount` requires `=.+` and reported **0 armed recovered** from the same container. **Count armed state by value — `grep -cE '^CLAUDISH_FAILOVER_[A-Z0-9_]+=.+'` — never by name.** Measured on the ai-01 sidecar over the 5 days to 2026-08-25: **124 requests served locally, 70 completed, 54 failed — every one of them the same `HTTP 429 [GLM Coding]`**, ~62s each walking the full retry ladder against a plan wall with nowhere to fall to. ⚠ That window is the **worst** one, not the norm, and it must not be quoted as a current loss rate: ai-01 re-counted by file on 2026-09-02 (623 req / 539 resp = 84 unanswered at most, which refuted its own finer-grained 132) and found the deficit concentrated on 20-25/08 — **28-30/08 and 02/09 show a deficit of zero, and the sidecar logged no 429 or 402 at all in the preceding 12h**. The benefit of arming is real but episodic, contingent on a wall being up at that moment. AUTONOMOUS is rare *and* it is exactly when the hub cannot help, so an unconfigured cascade there costs whole agent turns — one of the captures is four consecutive `msgs=2` startup attempts over four minutes, an agent that never booted. **Arm the cascade on sidecars too.** Note that arming is not enough on its own: `roleFromModelName()` matches only `opus|sonnet|haiku|fable`, so a client naming a bare provider id (`glm-5.2`) resolves to `null` and the cascade stays blind — either set `CLAUDISH_FAILOVER_ROLE_MODELS`, or have clients name roles neutrally.

### `CLAUDISH_QWEN_THINKING`

`disabled` (default) · `passthrough` · `budget:<n>`. **Unset is not neutral** — Qwen reasons by default, so unset means "think, at length", and the Token Plan bills on **output**. Re-read on **every request**, deliberately: the fleet flips this during a budget crunch, and a cached value would require restarting the proxy that is at that moment keeping everyone working.

**Two switches on two wires, and each endpoint ignores the other's form**: the OpenAI-compatible endpoint takes `enable_thinking` + `thinking_budget`, the Anthropic-compatible one takes the native `thinking` object. `QwenModelDialect.prepareRequest` branches on `ctx.wireFormat`; converting unconditionally (the old behavior) **deleted the only switch that works** on the Anthropic wire. Post-deploy check: on prompt `"Reponds exactement: ok"`, `input_tokens` dropping 67 → 31 proves the native switch reached Qwen. Full measurements: `docs/reference/budget-failover.md`.

### `CLAUDISH_GLM_THINKING`

`passthrough` (default) · `disabled`. GLM's switch is **binary** on the OpenAI wire (`thinking: {"type":"enabled"|"disabled"}`) — no budget control, unlike Qwen. Re-read on every request, same crunch-flip rationale.

**GLM thought by default and had no off switch at all**: `GLMModelDialect` deleted `thinking` unconditionally (a GLM-4.x-era artifact) and the OpenAI `buildPayload` never emitted the field either, so the client's ask was inert in both directions. Probed 2026-08-20 against the `gc@` Coding Plan (`glm-5.3`, prompt `"Reponds exactement: ok"`): field absent → 37 output tokens / 131 reasoning chars; `{"type":"disabled"}` → **3 tokens, 0 reasoning**; `budget_tokens` tolerated and **ignored**. `passthrough` preserves today's effective behavior; the `zai@` anthropic wire is unprobed, so only an explicit `disabled` sets the field there.

## OpenAI-Compatible Ingress (v7.2+)

`/v1/messages` is the **native** ingress; `POST /v1/chat/completions` is a **translated** one — converted to Anthropic shape, run through the **same** `getHandlerForRequest` → `ComposedHandler.handle` pipeline, then translated back. An OpenAI client (sk-agent, any `AsyncOpenAI` consumer) therefore inherits the routing cascade, budget failover, accounting and leak policy for free, by changing `base_url` alone.

- **Never-hang holds here too**: a malformed stream degrades to a single terminal chunk + `data: [DONE]`. `thinking` blocks surface as `reasoning_content`; `stop_reason` maps to `finish_reason`.
- **The relay is path-aware** — a sidecar forwards to the *same* route the client hit, so an OpenAI request reaches the hub's `/v1/chat/completions` in NOMINAL mode and the resilience model is preserved. The deep liveness probe stays on `/v1/messages`.
- Out of scope for now: `/v1/models` discovery, OpenAI web-search interception, per-role thinking policy on this path. Detail: `docs/reference/openai-ingress.md`.

## Traffic Analysis & Anthropic Policy

**Use the scripts, not hand-rolled grep.** The proxy log format produces false positives when grepped naively (`bytes=NNNN` matching error codes, timestamp digits matching `429`, `[msg:N]` body previews matching keywords — memory `proxy-log-monitoring`). The scripts encode the precise filters.

| Script | Use |
|---|---|
| `traffic-live.ps1` | live, from `docker logs` — fast; what the 6h surveillance cron runs |
| `traffic-summary.ps1` · `traffic-sessions.ps1` | rich detail (workspace, session, CC version, tokens) from `req-*.json` captures |
| `traffic-anthropic.ps1` | **"where is the Anthropic traffic coming from?"** — attributes each request by machine **and workspace** |
| `traffic-history.ps1` | past days, from `captures-*.7z` archives |
| `harness-injection-measure.py` | **"what does the harness actually cost in context?"** — pairs `req-*`/`resp-*` captures by `(pid, reqN)` and reports **characters injected per token** (~2,15); eight documented traps, each of which yields a wrong-but-plausible number; `--machine=` scopes to one machine (request-envelope field — a machine absent from the corpus yields 0 pairs, the negative control) and the per-file table carries **occ/req side by side** (occurrences vs distinct requests: MEMORY.md measured 4 341 occ on 2 459 req = 1,77/req). Trap 7 (#23 L1): the auto-load block is NOT guaranteed at `messages[0]` — CC ≥ 2.1.268 injects multiple `<system-reminder>`s and a condensation replaces message 0 with the summary, so the harness is re-injected further down; locating it only at position 0 kept the NATIVE lanes out of the measured population entirely (proven on `req-1-9999`: pairing + usage OK, detection False). `harness_blocks()` scans all messages/blocks and reports block count + positions per request |
| `native-consumption.py` | **"who burns the Anthropic-native budget?"** — pairs native captures with a time window (counters reset at restarts), attributes workspace (most-cited path root — these clients carry no working-directory env block), splits cron/interactive from the billing-header system block, and sums billed tokens (**OUT is the money**; harness rides in cache_read on this lane). Run on the loose day before 02:47 packing, or after pulling the day's archive from `G:\Mon Drive\Backups-Cloud\claudish` |
| `native-trend.py` | **"did cost-per-turn drift?"** — compares whole days on the native lane (extracts `resp-*-native-*.sse` from the archives itself), decomposing billed OUT into normal responses vs `<analysis>` compaction summaries (the expensive tail: ~10-15k tokens each), with ctx/req + %cache_read as control columns. Measured 2026-09-13: request count flat ×1.00 over 4 weeks while OUT/day ×1.95 — 70% of the drift is normal responses getting ~2× fatter, 30% compaction frequency ×2.13 (52→111/day, a consequence: fatter turns fill context twice as fast) |
| `lane-matrix.py` | **"what did each lane serve, per day, over months?"** — builds the multi-lane day/hour/lane response-count series (Epic #116 P1) into a sqlite store (`D:\claudish-captures\lane-series.sqlite`) from 7z archive **listings** — no decompression: timestamp, handler and model are all in the capture filename — plus the current loose day (`--loose-date`, idempotent re-run as the day grows). Every archive present gets a `days` row even at zero responses, so a lane outage stays distinguishable from a collection gap. Series semantics follow the `claudish_traffic` MCP contract. Machine attribution is NOT in the filename (it lives in the `req-*` envelope — see `native-consumption.py`); this organ counts lanes only |
| `compaction-trend.py` | **"what does re-reading context cost us?"** (#89) — compaction telemetry across ALL lanes from archive days: per Machine:Workspace:Harness, requests / compactions / ratio / re-paid tokens (in+cr+cc — the whole context re-read) / summary OUT. Detection is the last-user `"CRITICAL: Respond with TEXT"` signature — MEASURED 2026-09-16: all 235 compactions carried `stream:true`, so the flag is NOT a discriminator (and grep-anywhere matches 22× more, history echo). Usage from the paired resp (MAX per field — wire-openai emits a zero block first); on the containerized hub pid is ALWAYS 1 and reqN resets per restart, so counting is per FILE, never per (pid, reqN) |
| `compress-captures.ps1` · `claudish-watchdog.ps1` | nightly 7z compaction + GDrive backup (02:47 — moved off 04:17 on 2026-08-20: the 60-70 min run collided with the 04:00 container restart) · health check, proactive restart, hang recovery. **It is armed on the hub (po-2023) and running**: 661 log lines, a write every 15 min, and a logged proactive restart at 03:01 on 2026-09-01 (measured by po-2023, 2026-09-01). The earlier claim here — *never armed on any machine, verified 2026-08-23: no `Get-ScheduledTask` entry, 4 log lines from 31 May* — **was wrong on both of its legs**, and each leg is a trap worth keeping: (a) the documented install is `/ru SYSTEM` (header line 10), and a non-elevated `Get-ScheduledTask` / `schtasks /query` is **blind to a SYSTEM task** — it exits 1 with no output, indistinguishable from absence; (b) `$LogPath` and `$StateFile` are hardcoded to `C:\Users\jsboi\.claudish\` (lines 17, 31, 154), so on any machine whose operator is not `jsboi` the log is written to a directory that does not exist — **counting its lines measures nothing there**. Verified on myia-ai-01, 2026-09-02: `C:\Users\jsboi` is absent, and whether the watchdog runs on that machine is therefore **still unknown** — neither check can answer it without elevation. ⚠ The hardcoding is deliberate (under SYSTEM, `$env:USERPROFILE` is the *system* profile, not the operator's), so replacing it with `$env:USERPROFILE` would break the one install that demonstrably works. Since #37 it classifies probe failures (429/402/529 → DEGRADED, no restart) and only restarts proactively in the 03:00-06:00 quiet window. **2026-09-20 ×2 (po-2025): Docker Desktop localhost-forwarder wedge** — port 3000 LISTENING, HTTP dead on loopback ONLY, published `0.0.0.0` binding serving (WAN/ARR survived both episodes, every local client died up to ~7h; container restarts were useless ×13, host reboot was the only lever, and the wedge followed container-restart events by minutes both times). **A first attempt at automating recovery caused a worse outage than the one it targeted; it was withdrawn the same day (reverted in `cb3e690`/`cb4f214`), then rebuilt with the guards and tests it never had.** `105a2c8`/`2c4d12c` moved the watchdog/drain probes onto a per-machine `base-url.txt` and added a loopback-watch plus a `Restart-DockerEngine` escalation. At 13:18 it fired on a **real** wedge (`[::1]:3000` dead while the serving path answered a full tool-call stream — third occurrence that day, ~20 min after an engine restart), killed the Docker backend and the WSL VM, then **could not relaunch Docker Desktop** (`Impossible de trouver un paramètre correspondant au nom « LogonType »`). The host was left with no engine at all — every workspace `ConnectionRefused`, on this machine and on clients pointed at it — until a manual relaunch. Strictly worse than the wedge, which leaves 127.0.0.1 and the LAN serving. The root cause outlives that attempt and is worth knowing on its own: **`Register-ScheduledTask` has no `-LogonType` parameter** — it belongs to `New-ScheduledTaskPrincipal` — so the Docker Desktop relaunch inside `Start-DockerEngine` **has never worked on any machine**, on `main`, for its whole existence. It always threw, its own `catch` swallowed the throw, and it logged the reassuring "could not launch Docker Desktop". That single latent defect explains both the 13:18 outage and the 2026-08-29 double reboot: every path that stops the engine has been unable to restart it. Three lessons, all structural: (a) **a remediation that tears a component down must be proven able to rebuild it before it is allowed to tear anything down** — the teardown half was tested, the rebuild half never was, and the verification that was performed (probe does not fire when healthy, 4 addresses at 200, one cycle exit 0) could not have caught it; (b) changing the probe from `localhost` to `[::1]` silently broke the opt-out guard `if ($loopbackUrl -ne $ProxyUrl)`, which disarmed the watch **only because the two strings were equal** on a machine with no `base-url.txt` — once the probe became `[::1]` they could never be equal, so the watch armed on every machine, including ones that never opted in; **a guard expressed as a string comparison between two values that merely happen to coincide is not a guard**; (c) the **detection** is sound and is the only part worth rebuilding — `[::1]` dead while the serving path answers is the exact signature, confirmed three times in one day — and it is the only part that ships: **there is no actuator at all** (#172 AC4). **What ships now is a detector and nothing else** (`scripts/lib/claudish-engine.psm1`, a testable module both the watchdog and the drain import, + `scripts/tests/*.Tests.ps1`, 101 Pester tests in the two suites touched here). The terminal verdict of the wedge path is `escalate` — a log line telling the operator **not** to restart the container, because 13 restarts proved it cannot help. **Zero engine, container or service actions anywhere in that path**, and a static AST test enforces it: a reviewer greps the wedge functions and finds no `Restart-*`, no `Stop-*`, no `-Verb RunAs`, no `docker stop|restart|kill` (the test carries a positive control, because a detector that silently matches nothing proves nothing — injecting a `Restart-Service` into the escalation branch makes it fail naming `Invoke-LoopbackWedgeWatch:543 Restart-Service`). The discriminant is what makes the verdict safe to act on: **`[::1]` dead while 127.0.0.1 *or* the LAN answers is a forwarder wedge; all three families dead is the hub itself** and belongs to the hang path, where a restart *is* the fix — mislabelling that as a wedge would tell the operator to stand down during an ordinary outage. Gated **off by default** on an explicit opt-in file (`<ClaudishHome>\wedge-watch.enabled` containing `enabled`), checked **before any probe runs**, so a machine that never opted in is not even measured; an empty or wrong-content file does **not** arm — the previous guard armed on an accidental string equality. **Every unknown fails safe**: no opt-in file, no visible `[::1]` listener, a throwing probe — all route to "do nothing". With no `base-url.txt` and no opt-in file, behaviour is byte-identical to before on every machine, which is the state of the entire fleet today (verified on the production hub 2026-09-20 14:16: a full cycle under 5.1, exit 0, **prod log delta 0**, and the watch emitted no line at all). `Test-EngineRelaunchReady` survives as the preflight for the *pre-existing* `Start-DockerEngine` — which is where the `-LogonType` defect lived — and is reachable only from that path, never from the wedge path. Since #185 it is ALSO exercised during a HEALTHY cycle (`Invoke-RelaunchPreflightWatch`, called right after the wedge watch): the #181 execute probe needed the Desktop GUI up while its only call site required it down, so it was structurally unreachable in production — the healthy cycle is the only place it can run. ⚠ **Its first live exercise (po-2025, 2026-09-21 11:02Z, #199) refuted the "invoking it is a free no-op second GUI instance" premise**: the relaunch task's action IS the GUI executable, so its instance stays Running for the GUI's lifetime, and `MultipleInstances=IgnoreNew` makes the scheduler REFUSE the second start (0x800710E0) *before the action runs* — no new process ever appears. Control comparison produced by production: the same task SUCCEEDED at the 20/09 19:47 recovery (GUI down; processes born 19:47:11–18) and was REFUSED at 21/09 11:02 (GUI up). Two rules follow (#199): (1) **read `LastTaskResult` as `[long]`, never `[int]`** — every real Windows failure HRESULT has the high bit set, an `[int]` cast THROWS, and the check degrades to `execute:ERROR` hiding the code from you (the unit suite missed it because its only failing fixture was `result=1`, Int32-shaped, and the test helper itself was `[int]` — a harness that shares the code's type assumption is blind to the code's type defect); (2) **0x800710E0 + task State=Running is `execute:SKIPPED(already-running)`**, `Ready=$true`, reason "no fresh exercise performed" — neither a pass claimed nor a failure invented — while the SAME code with no live instance stays `FAILED` (the state guard, on a `$TaskStateReader` seam, is what keeps that mapping from swallowing a genuine refusal; decimal `execute:FAILED(result=N)` tokens unchanged). **#205 then INVERTED the execute condition**: invoking while the GUI runs returns 0 by **single-instance forward** — the born process is a renderer child of the live GUI (measured po-2025 21/09: parent = pre-existing GUI, `--type=renderer`; zero processes born in the 7 h 50 before the probe), so a GUI-up `execute:OK` proves "the scheduler can start the action", never "the action can raise a cold engine", which is the exact class `-LogonType` hid — **every GUI-up `execute:OK` ever obtained is a weak green, not citable as proof**. Since #205: GUI running → `execute:SKIPPED(gui-running)`, `Ready=$true`, "no fresh exercise performed", **not invoking at all** (also stops the resident renderer); GUI absent → the measured invoke, which cold-starts the engine — the only branch where the measurement means anything, and precisely when a relaunch might be needed. **The gui-running SKIP does NOT consume the daily probe** (`relaunchProbeDate` unconsumed, ranks with the THROW branch): on a GUI-autostart machine a consuming skip would certify a daily probe that never measured anything — the same false green one level up. `SKIPPED(already-running)` (#199) stays a measured verdict and keeps consuming. **Default-OFF on its OWN consent file** (`<ClaudishHome>\relaunch-preflight.enabled`, review of #188): the gate is read before anything else, so a merge changes nothing on any machine until an operator creates the file — deliberately NOT shared with `wedge-watch.enabled`, since that consent covers HTTP GETs while this one registers a scheduled task and launches an elevated process, and one consent must not silently carry the other (`Test-ClaudishOptIn` holds the token semantics once; both wrappers are pinned from both sides by mutation). Rate-limited to ONE measured verdict per UTC day (state field `relaunchProbeDate`) — Docker Desktop / Windows updates are weeks apart, so daily catches drift within a day without the 96/day churn; a measured verdict consumes the day (a failed probe escalated every cycle would be noise), a THROW does not (retries next cycle, logged as 'not measured', never as a fault). Failing verdict = `RELAUNCH-PREFLIGHT FAILED` + `ESCALATE` log lines, zero action, under its own AST guard with positive control (no Restart-*/Stop-*/wsl/-Verb/docker stop|restart|kill, and no Invoke-ClaudishDrainedRestart/Start-DockerEngine by indirection). Two environment traps found while building it, each of which had already produced a wrong answer: (i) **production runs Windows PowerShell 5.1**, not pwsh 7 (`schtasks /tr "powershell …"`), and `http://[::1]:3000/health` returns **HTTP 400 under 5.1 and 200 under pwsh 7 at the same instant** — so a `== 200` check invents a wedge on a healthy machine under the only interpreter that matters. A wedge is **no HTTP answer at all**; any status code proves the pipe is alive (`Test-HttpAlive`), the suite is run under **both** interpreters, and a parse guard pins the scheduled-task scripts to 5.1-compatible syntax (`??` / `?.` / ternary are a production-only parse failure there — `CaptureUtils.psm1` is pwsh-7-only by design and is the one documented exception). (ii) **`Win32_ComputerSystem.UserName` is empty under RDP** (it reports the console session only) and `quser` output is **localized** — only the session-name column (`console`, `rdp-tcp#N`) is a stable literal, so that is what the interactive-user resolution keys on. ⚠ Also measured and still true: **a loopback probe must name `[::1]` explicitly, never `localhost`** — `wslrelay` owns `[::1]:3000` alone while `com.docker.backend`'s `[::]:3000` wildcard keeps serving 127.0.0.1 and the LAN, and .NET 5+ (pwsh 7) falls back IPv6→IPv4 fast enough that a `localhost` probe reports healthy straight through the outage, while a Node client resolving `::1` first takes an immediate `ConnectionRefused`. **`networkingMode=mirrored` is a tested dead end** (same day): it removes `wslrelay`, and with it `[::1]:3000`, turning an intermittent loopback wedge into a permanent one — every `localhost`-naming client dead, 2 host reboots. It does **not** stop the service: the LAN path served real tool-call streams throughout. The measurement that said otherwise was `Get-NetTCPConnection -LocalPort 3000 -State Listen` returning **empty**, which is **invalid under mirrored** (sockets live in the mirrored WSL netns and the Windows TCP table never enumerates them): empty means "not visible", never "not serving" — the same class of error as counting armed cascades by name instead of by value. And a one-shot elimination test on a critical path carries its **rollback automatic on failure**: that one logged `ROLLBACK RECOMMENDED` and stopped there, so the outage ran until a human read the log |
| `claudish-drain.ps1` | **restart without dropping agent turns.** Run it standalone in place of `docker restart`, or dot-source it for `Invoke-ClaudishDrainedRestart` — which needs **`-Recreate -EnvFile <path>`** to deploy, since a restart reloads neither the image nor `.env`. `-EnvFile` is mandatory with `-Recreate` and fails fast if missing: docker compose interpolates every `${VAR:-}` from that file, the hub's real one lives outside the compose dir, and the compose dir's own `.env` holds only `CLAUDISH_PROXY_KEY` — a bare recreate emptied every `CLAUDISH_FAILOVER_*` (2026-09-07). Since #141 the refuse is stronger: an env file that **exists but carries no armed `CLAUDISH_FAILOVER_*` while the live container does** is also refused (an env file can be present and still gut the cascades — it merely lacks them); `install-sidecar.ps1` preserves runtime-policy lines on rewrite and `-RebuildEnvFromContainer` recovers an armed state that lives only in the container |
| `docker-events-collect.ps1` | **persist container lifecycle events before `docker events` forgets them** (#169): the daemon's ring buffer evicts in ~35 min, so post-hoc attribution of a restart window is impossible — the 2026-09-20 hub incident (5-6 restart windows 03:49Z→09:07Z + recreate 09:13Z) lost every per-window exit code and actor identity, with `RestartCount=0` throughout because the gestures were manual `compose up/start`. One tick = a **bounded `docker events` window** (the process is killed at `-WindowSec`; measured on Docker Desktop 29.8.0 it never self-terminates in any argument form, so the kill IS the close), deduped across windows by `timeNano|action|container` fingerprint, appended as the provider's own bytes to `<ClaudishHome>\docker-events.log` (NDJSON, no BOM — `[System.IO.File]::AppendAllText` with `UTF8Encoding($false)`, because `Add-Content -Encoding UTF8` writes a BOM on 5.1 that corrupts exactly the first event), rotated at 5 MB, with `docker-events-state.json` recording **one tick per run, bounded** — that history is what separates a DEAD collector from a QUIET daemon, the same trap as `grep -L` on a zero-byte capture. 🔴 **It writes lifecycle events ONLY, and that is a security property — never widen the filter back to a bare `type=container` (#192).** That filter admits `exec_create`/`exec_start`, and docker puts the **executed command line** in their `Action` field, so every container healthcheck passing a credential as an argument was appended in clear text to a persisted, rotated log every 15 min — for every container on the host, including other workspaces'. Measured read-only on a live daemon, two 8 s windows over the same `--since`: **313 events / 203 carrying a command line → 0 / 0**, the before-window's distinct actions being `exec_create`, `exec_die`, `exec_start` **and nothing else**. The same missing filter is why it captured **0 lifecycle of 313** — none of what it exists for and all of what it must never touch — and why it could **manufacture its own headline verdict**: healthchecks fire across every container continuously, so ≥5 distinct containers inside 1 s was reachable on a perfectly calm host. Hence the guard runs **before the verdict**, not merely before the write. Two layers answering two different questions: `Get-DockerEventsFilterArgs` (bytes never read) **and** `Select-LifecycleDockerEvents` (never written on a daemon that honors the request differently, testable with no docker at all), with `exec_*` refused **unconditionally, above any allowlist** — the allowlist says which events are *useful*, that says which are never *safe to write down*. `health_status` is deliberately out despite carrying no command line: at one check per 30 s across ~38 containers it would dominate the file and the 5 MB rotation would evict the restart events #169 preserves — a collector whose noise pushes out its own signal has the defect it was built to fix. ⚠ **Anyone who installed the task from the #169 header line before 2026-09-21 should treat the existing log as sensitive** and never paste it into an issue or a chat. Four rules, each load-bearing and mutation-tested: (1) **zero actuator** — an AST test refuses any `Restart-*`/`Stop-*`/`docker stop\|restart\|kill`/`-Verb RunAs` in the file (positive control included), because a collector that "helpfully" restarts is the #173 teardown-without-rebuild shape; (2) **the group verdict never names an actor** — ≥5 distinct containers within 1 s is `shared-cause` (host/daemon), else `targeted`; reading simultaneity as a targeted action was the 2026-08-30 false attribution; (3) **a failed invocation holds the watermark** — advancing on a window that never rendered would silently write off the unread range, and the ambient-culture round-trip (`ConvertFrom-Json` → `[datetime]` → `[string]` → `"09/20/2026 17:34:59"` → docker refuses) broke every tick after the first on the live E2E; `ConvertTo-DockerSinceInstant` renders invariant RFC3339 and unreadable watermarks fall back to the lookback, i.e. toward re-reading. Live proof po-2024 2026-09-20: two ticks around a real `create`+`rm` captured 4 events, and tick 2 showed `events=3 written=2 skipped=1` — the boundary duplicate the overlapping watermark produces, deduped for real. Install: `schtasks /create /tn ClaudishDockerEvents /tr "powershell -ExecutionPolicy Bypass -File <repo>\scripts\docker-events-collect.ps1" /sc minute /mo 15 /ru SYSTEM /rl HIGHEST /f` (+ `-ClaudishHome` where the operator is not `jsboi`). Tests: `scripts/tests/claudish-docker-events.Tests.ps1` (45 as of 2026-09-21; a bare count here goes stale, so treat it as dated) under both interpreters |

- 🔴 **`compress-captures.ps1` on a second machine needs `-MachineTag`, and refuses to run without it (#201).** The off-site directory is a namespace **shared by the whole fleet**; the archive name carried no machine dimension, the upload is `Copy-Item -Force`, and the confirmation compares the destination to **the file this run just wrote** — a check that cannot fail. With the default `-KeepLocalDays 0` off-site is the *only* home, so a second producer overwrote the first machine's day, "confirmed" against its own bytes, and purged locally, while the first had purged the night before: the day was gone everywhere, and the re-upload pass cannot recover it (it only retries archives still present **locally**). Measured on GDrive after the first tagged run, 2026-09-21: `captures-2026-09-20.7z` **294.0 MB** (the producer's, intact) beside `captures-2026-09-20-ai-01.7z` **0.6 MB** — untagged, **0.6 MB would have overwritten 294 MB**, a factor of 490, silently. Since `5682790` a run that would join the shared namespace anonymously exits **3 before archiving or copying anything**; `-AllowUntaggedSharedArchive` is the opt-in for the **one** machine owning the existing untagged history, so its names — and every reader matching `captures-<day>.7z` — are unchanged. Two consequences that look like style and are not: (a) the destructive configuration **was the default**, so the refusal has to be the default too — a warning in an unattended nightly job is a line nobody reads; (b) the re-upload and retention-purge passes are scoped to archives the run **owns**, and a name the parser cannot read is *skipped*, never guessed at, before a delete. 🔴 **The ALLOWED untagged write could still overwrite another producer's archive — that risk ran nightly since 2026-09-06, 14 days of outage forensics lost (#208, fixed the same week it was measured).** `-AllowUntaggedSharedArchive` said "you may use this name" and behaved as "you may overwrite whatever is there": the legacy producer and a second untagged machine (a relay whose 2-5 MB/day captures ARE its forward-path outage trail — a NOMINAL relay writes no capture at all, so every file on it is a forward-failure artifact) wrote the same `captures-<day>.7z` at the same minute; the relay landed first (~4 min), the big corpus overwrote it ~2 h later, retention=0 made off-site the only home, and the post-write confirmation compared against the run's own freshly-written bytes. Measured day by day: 14 relay uploads, 0 survivors (13 later found recoverable as Drive prior versions within the 30-day window; the 14th night produced no relay upload at all). Since #208 every off-site write reads the destination **prior** to the copy — `Get-OffsiteWriteVerdict`, at BOTH write sites (compress-loop upload and re-upload pass): absent → write; **identical size → idempotent** (a retried night must not start failing); **different size → REFUSE naming both sizes** (loud `REFUSE` log line, counted as an error so the scheduler sees a non-zero exit). A different size on a name this run owns has no benign interpretation, and the size guard holds even when two machines are configured on the SAME name — strictly stronger than a tag. **The local purge stays downstream of that decision**: the purge already required an exact destination size match before deleting local, so a refused archive survives locally and the next run re-runs the decision — pinned by test, because that half is what turns an overwrite into data loss. Process rule (#208 AC6, the gap that let the collision run a full day after the fix existed): **a fix protecting a shared namespace must enumerate its PRODUCERS, not its repositories** — an up-to-date tree on one machine says nothing about what the others execute; the affected producer's script was three weeks stale. ⚠ The readers (`traffic-history.ps1`, `lane-matrix.py`) still match the untagged spelling only and are **blind to tagged archives** — they will silently return *less*, which reads as a quiet period rather than a defect (**#203**). ⚠ `Register-ScheduledTask` has **no `-WhatIf`** (same class as its missing `-LogonType`), so the dry-run for arming this task cannot go through the cmdlet: resolve and print every value, stop, then verify by **re-reading the registered task against the values intended** — not against the gesture's own output, which is the very defect above.
- **`traffic-live.ps1 -Container` defaults to `claudish-proxy` (the hub name).** On a sidecar machine you MUST pass `-Container claudish-sidecar`, or the script exits 1 with `No such container`.
- **`-Hours N` is the window you get again — but read the mode line.** `--since` was unusable on this Docker Desktop after a 2026-07-01 clock skew (GOTCHA #2), so the window was approximated by tail depth at ~8k lines/hour — the *hub's* peak rate. A quiet container produces far less, so the same tail reached much further back: on the ai-01 sidecar `-Hours 12` returned **9.25 days** under a header saying "last 12h", and counted **67 rate-limits dated 21-27/08 as if they were recent** — the difference between "the AUTONOMOUS path is failing" and "it has been clean for three days". Re-measured 2026-08-30 (Docker 29.7.2): `--since` is correct again and is now primary. `--tail` survives as a fallback that fires only on the GOTCHA #2 signature — `--since` empty *while* the container's newest line is inside the window — so a genuinely idle container still exits 0 quietly. The script prints the mode it used and the observed span; a short span in `since` mode just means a quiet container, not a defect.
- **On a NOMINAL sidecar, ~0 requests is the correct answer, not a broken script**: a relayed request writes no capture and emits no `[Request]` line, so local traffic analysis is blind there by construction. **The blind spot is not total, though — the relay still emits one `[ttft]` line per *forwarded* request**, so on a relay machine `docker logs … | grep '\[ttft\]'` is the instrument that sees real volume. Measured on po-203, 2026-09-13: a 2h window held **733 `[ttft]` lines against 0 `[Request]` and 0 `[resp]`** (nothing served locally) ⇒ counting `[Request]` alone undercounts a sidecar by ~100× over 6h (1 653 vs 21). The `model=` there is the **client's requested id, pre-remap** — so `claude-sonnet-4-6`, a retired id absent from every catalog, can legitimately dominate those lines while being served by a budget model. Verify by timestamp alignment before concluding a lane is quiet; a `msSinceProgress` snapshot says nothing about forwarded volume.
- **Counting AUTONOMOUS episodes by grepping `AUTONOMOUS` overcounts ~6×** — the string also appears in the `[x/2 before AUTONOMOUS]` suffix of every *fallback* line. Transitions are the lines that **end** with `→ AUTONOMOUS` (`grep 'AUTONOMOUS$'`); `healthy again` lines are recoveries. Measured po-203, 2026-09-14: 261 raw matches vs **40 true transitions in ~47h** (21 on the 14th, escalating through the afternoon). The driver is structural, not a local fault: `forwardToUpstream`'s **30s header budget** sits against a hub header-phase latency of **p50 1.6s / p90 8.7s / p99 21s / max 69s** (n=4569 `[ttft] headers=` samples) — over 1% of forwards blow the budget, fall to local serving (each costing the client a 30s stall), and two failures inside the window flip the relay AUTONOMOUS (plus isolated `hub HTTP 500`s). A relay's flap rate therefore tracks **hub header latency**, and is a hub-load signal before it is a sidecar problem.
- **Policy: Anthropic-billed models (Opus, Fable, Sonnet) must come from `myia-ai-01` only.** Verdicts: `[OK]` ai-01 · `[REVIEW]` po-2025, which may run an authorized Safari/agent-sdk workflow — **confirm with the user before calling it a leak** (2026-06-21: six false WARNs were raised by skipping that step) · `[LEAK]` any other machine.
- **`cc_is_subagent=true` is the leak signature, and it lives in the request body, not stdout** — `docker logs` alone cannot see it; inspect a capture. The `[LEAK-SUBAGENT]` (rogue Opus sub-agent, exit 1) vs `[REVIEW-INTERACTIVE]` (user driving their own session, exit 0) split is what stops the tool crying wolf on legitimate dev sessions.
- **A confirmed sub-agent leak is fixed client-side** — a `~/.claude/rules/` rule pinning `model: "sonnet"` on spawns, reserving Opus for genuinely complex tasks. Not fixable in the proxy.

Capture format, archive/GDrive retention, device-id attribution, exact commands: `docs/reference/traffic-analysis.md`.

## Channel Mode (MCP, v6.4.0+)

Async model sessions with push notifications, built on the low-level `Server` class declaring `experimental: { 'claude/channel': {} }`. 11 tools, gated by `CLAUDISH_MCP_TOOLS` (`all` default · `low-level` · `agentic` · `channel`). The wire contract is pinned by `channel-wire-format.test.ts`.

- **`meta` keys must match `[a-zA-Z0-9_]+`** — Claude Code **silently drops** keys with hyphens or other characters. Keep new `extraMeta` keys underscore-only.
- **Emitting a correct frame is not enough.** Rendering is gated client-side: CC ≥ v2.1.80; Anthropic auth via claude.ai or Console key (not Bedrock/Vertex/Foundry); an **interactive** session (in `-p` mode registration never runs and frames are silently dropped); the server declared in `.mcp.json` / `~/.claude.json` (**`--mcp-config` is not consulted by the channel resolver**); and the server **named in `--channels`** or `--dangerously-load-development-channels`.
- `CLAUDISH_CHANNEL_TRACE=1` (+ `CLAUDISH_CHANNEL_TRACE_FILE` when the host swallows stderr) traces producer → bridge → wire, so a server-side drop is distinguishable from client-side gating. Off by default, zero production overhead.

Full contract, launch commands, diagnostic scripts: `docs/reference/channel-mode.md`. Tests: `bun test --cwd . ./packages/cli/src/channel/*.test.ts` (65 across 5 files).

## Debugging & Tests

Failed-translation workflow: `claudish --model X --debug "say hello"` → `claudish --probe X` (verify the composition) → `/debug-logs logs/claudish_*.log` (diagnoses the failure mode, extracts SSE fixtures, writes a regression test) → `bun test packages/cli/src/format-translation.test.ts`. Manual extraction: `bun run packages/cli/src/test-fixtures/extract-sse-from-log.ts logs/claudish_*.log`.

**Keep debug logs verbose** — empty chunks and raw deltas included; that is what makes real model streaming behavior legible. Suppress noise at the registration/initialization level (e.g. conditional middleware), **never at the streaming-data level**. Under `--debug` both parsers emit greppable `[SSE:openai]` / `[SSE:anthropic]` lines, which become test fixtures.

`format-translation.test.ts` replays `.sse` fixtures from `test-fixtures/sse-responses/` through the stream parser. Helpers: `parseClaudeSseStream()`, `extractText()`, `extractToolNames()`, `extractStopReason()`, `fixtureToResponse()`. Add regressions as a `describe("Regression: <model>")` block (template at the bottom of the file).

**PowerShell operations scripts have their own suite**: `bun run test:scripts` (pwsh 7) and `bun run test:scripts:win51` (Windows PowerShell 5.1 — **the interpreter the scheduled tasks actually use**; run both, they do not agree, and the one that ships is 5.1). Pure logic lives in `scripts/lib/claudish-engine.psm1` so it can be exercised without touching Docker, WSL, the task scheduler or the network; the watchdog's glue is tested by dot-sourcing it with an injected `-ClaudishHome` and injected probes. ⚠ **Dot-sourcing `claudish-drain.ps1` runs its `param()` defaults in the caller's scope and clobbers `$ClaudishHome`** — the watchdog saves and restores it around the dot-source, because without that a "sandboxed" run wrote its state to the sandbox and its log to the *production* log (measured 2026-09-20: 2 stray prod lines). Any test claiming a sandbox must assert on the log path too, or it is lying about its blast radius.

Detail: `docs/reference/proxy-pipeline-notes.md`.

## Learned Preferences

### Tools & Commands
<!-- learned: 2026-03-28 session: 03cd7cc5 source: repeated_pattern -->
- Use `bun` for all package management and scripts (`bun run build`, `bun test`, etc.) — not npm or yarn
<!-- learned: 2026-04-06 session: df311293 source: repeated_pattern -->
- Use Grep/grep tool for code investigation instead of mnemex — prefer built-in search tools during investigation phases

### Workflow
<!-- learned: 2026-04-06 session: df311293 source: explicit_rule -->
- Don't run claudish directly in main bash — use dedicated channel sessions or `/delegate`
